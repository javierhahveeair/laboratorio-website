/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: false,
        domains: ['api.placeholder'], // Add other domains if needed
    }
}

module.exports = nextConfig
